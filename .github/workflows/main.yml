# Experimental branch only: the full upstream workflow is preserved at
# docs/ci/legacy-main.yml (identical Git blob, no content lost).
# Restore it before merging this feature into main. The main branch is unchanged.
# No automatic triggers: use duckdb-2dev-fast.yml and duckdb-2dev-coi.yml.
name: Legacy matrix (paused on DuckDB 2 alpha branch)
on:
  workflow_dispatch:

jobs:
  legacy-paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Legacy matrix intentionally paused on the experimental branch.'
